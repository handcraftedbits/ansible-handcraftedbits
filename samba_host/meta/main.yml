---
dependencies:
  - role: handcraftedbits/docker_host
    docker_host_hostname: "{{samba_host_hostname}}"
    docker_host_password: "{{samba_host_password}}"
    docker_host_username: "{{samba_host_username}}"